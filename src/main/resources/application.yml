springdoc:
  packages-to-scan: hello.neardeal_server
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
  swagger-ui:
    enabled: true                # Swagger UI ???? : ?? ?? => <http://localhost:8080/swagger-ui/index.html>
    path: /swagger               # Swagger UI ?? ?? ?? => <http://localhost:8080/swagger> => ? ??? ????? ?
    tags-sorter: alpha           # alpha: ??? ? ?? ??, method: HTTP Method ? ??
    operations-sorter: method    # alpha: ??? ? ?? ??, method: HTTP Method ? ??